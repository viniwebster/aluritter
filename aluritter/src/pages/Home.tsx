import styled from "styled-components"

const StyledTitle = styled.h1`
  font-size: 32px;
`

const Home = () => {
  return(
    <StyledTitle>
      Bem vindo ao aluritter
    </StyledTitle>
  )
}

export default Home